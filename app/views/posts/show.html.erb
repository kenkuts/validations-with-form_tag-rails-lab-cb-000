<h2><%= @post.title %></h2>
<p>A <%= @post.category %> post.</p>
<p><%= @post.content %></p>

<%= button_to "Edit Post", edit_post_path(@post), html_options = { :method => :get } %>
<%= button_to "List Posts", posts_path, html_options = { :method => :get } %>
